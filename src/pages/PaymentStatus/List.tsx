import { PaymentStatusListContainer } from "../../containers/PaymentStatus";

const List: React.FC = () => {
  return (
    <>
      {/* Start: PaymentStatusListContainer */}
      <PaymentStatusListContainer />
      {/* End: PaymentStatusListContainer */}
    </>
  );
};

export default List